# Runtime 知识库（给人看的指针）

NapCat / SnowLuma **运行语义**（路由、WebUI 鉴权、Docker compose、daemon 模型）的权威短文档在 **本机**：

`.claude/kb/`（不进 git，与 `.claude/STATE.md` 同域）

Agent 开工前应 Read 该目录； slash 命令：`/load-runtime-kb`、`/sync-runtime-kb`（对照 `.references/*` 核对）。

人读长文仍用：

- `docs/context/remote-docker-bot-parity.md` — Docker 体验对齐方案
- `docs/context/lessons.md` — 踩坑铁律
- `docs/context/codemap.md` — 代码落点