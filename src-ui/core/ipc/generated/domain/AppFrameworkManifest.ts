// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AppFrameworkId } from "./AppFrameworkId";
import type { AppPlacement } from "./AppPlacement";
import type { AppStoreResource } from "./AppStoreResource";
import type { AppWebUiAuthKind } from "./AppWebUiAuthKind";
import type { OneBotLinkMode } from "./OneBotLinkMode";

/**
 * 应用端框架清单（静态，由各框架模块给出；UI 直接消费，不做派生）。
 */
export type AppFrameworkManifest = { id: AppFrameworkId, 
/**
 * UI 显示名（"Karin" / "NoneBot2"）
 */
display_name: string, 
/**
 * 一行简介
 */
description: string, repo_url?: string | null, docs_url?: string | null, 
/**
 * 已开的 placement 子集
 */
supported_placements: Array<AppPlacement>, 
/**
 * 框架惯例端口（Karin 7777 / NoneBot 8080）。新建实例不再使用，只作文档兼容。
 */
default_port: number, 
/**
 * 是否有可打开的 WebUI
 */
has_webui: boolean, 
/**
 * 支持的对接拓扑；首位为推荐
 */
link_modes: Array<OneBotLinkMode>, 
/**
 * 承载安装/探测的组件 wire 字面量（如 "karin"），与 ComponentId 的 serde 字面量一致
 */
component_id: string, 
/**
 * 运行时依赖组件的 wire 字面量（如 "nodejs" / "uv"）；组件页据此展示「依赖是否就绪」。
 * 真相仍是 Component::requirements()，注册表测试保证两者一致
 */
runtime_component_ids: Array<string>, 
/**
 * 商店 Tab 对应的资源；空 = 无商店。UI 按此挂 Tab，不写框架名。
 */
store_resources: Array<AppStoreResource>, 
/**
 * 新建实例是否展示「一并安装渲染器」
 */
has_install_renderer: boolean, 
/**
 * WebUI 登录方式；决定新建对话框是否收账号密码、打开时弹不弹账号框
 */
webui_auth: AppWebUiAuthKind, };
