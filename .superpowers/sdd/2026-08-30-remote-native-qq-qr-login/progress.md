# SDD ledger — plan: docs/superpowers/plans/2026-08-30-remote-native-qq-qr-login.md

Baseline setup: `pnpm install --frozen-lockfile` completed; `cargo check -p ncd-tauri` completed with existing warnings only (ncd-network proxy constants not injected; ts-rs transparent-attribute warnings).
Gate evidence reviewed outside the repository: `work/qq-nt-qr-navigation-2026-08-30/report/qr-login-static-report.md` and E-001 through E-004. This is read-only IDA/static evidence for QQ QR data acquisition, not dynamic Kunming UI calibration.
Navigation gate: BLOCKED. No authorized Kunming dynamic evidence or `qq-qr-navigation-calibration.md` was available. Missing: QQ build identity; display, resolution, and DPI; initial login state; every anchor; action and wait conditions; accepted theme/language/scale/build/window variants; failure branches; and repeated-run results.
Decoder gate: BLOCKED/unverified. No real QQ QR screenshot sample or payload was available outside the repository, so no decoder candidate was run and no decode success is claimed.
Production Tasks 1–8: not started; no production source, SnowLuma backend, QQ, SnowLuma asset, or noVNC files modified.
Follow-up SSH target: `kunming-4-8` was reachable with `ssh -o BatchMode=yes -o ConnectTimeout=10 kunming-4-8 'printf "ssh-ok\\n"'`; result `ssh-ok`. No packages were installed and no remote files were modified.
Bounded remote facts: `/opt/QQ/resources/app/package.json` reports `version=3.2.32-52194`; `/root/Napcat/opt/QQ/resources/app/package.json` is unavailable. Xvfb command is `Xvfb :0 -screen 0 1280x720x24 -nolisten tcp`; `DISPLAY=:0`, `/root/.Xauthority`, `xdpyinfo` reports 1280x720 and 100x100 DPI. No explicit toolkit scaling was observable (`xrdb` returned no matching values; `xsettings` unavailable).
Observed window anchor: `xwininfo -root -tree` and `xprop -display :0 -id 0xc00003 ...` identify a viewable QQ window, class/name `qq`/`QQ`, `_NET_WM_PID=1990534`, geometry `(480,130) 320x460`. Helper QQ-class windows also exist; no semantic login-control anchors were observable without UI automation.
Capture observations were repeated twice using transient in-memory `PIL.ImageGrab.grab()` only: both runs returned `size=1280x720 mode=RGB`; no screenshot was written or retained. The same QQ window identity and geometry were observed in both runs. These are repeated observations, not successful navigation runs.
Observable capability/failure branches: `xprop` and `xwininfo` are present; `wmctrl`, `xdotool`, `scrot`, `gnome-screenshot`, ImageMagick `import`, `xwd`, and `convert` are missing. `DISPLAY=:1` fails exactly with `xprop: unable to open display ':1'`. Pillow/ImageGrab and `libXtst.so.6` are available; `python-xlib` is missing. No action sequence or existing dynamic-test procedure was available to establish safe click/key coordinates, post-action waits, or change detection.
Before this follow-up, no action sequence or existing dynamic-test procedure was available to establish safe click/key coordinates, post-action waits, or change detection; this does not describe the follow-up attempt below.
Navigation gate: remains BLOCKED. Two repeated read-only observations improve evidence but do not satisfy the required repeated navigation runs or calibration artifact.
Decoder gate: remains BLOCKED/unverified; no real QR sample was accessed or decoded.
Workspace prepared for Task 0 only. Production implementation Tasks 1–8 are not started.
Follow-up dynamic calibration attempted against authorized SSH target `kunming-4-8` with `DISPLAY=:0`, transient in-memory Pillow `ImageGrab`, and ctypes `libX11`/`libXtst` XTest. No remote files, production code, SnowLuma, QQ, screenshots, or QR payloads were modified or persisted.
Follow-up facts revalidated: QQ build `/opt/QQ/resources/app/package.json` version `3.2.32-52194`; display `:0`, resolution `1280x720`, DPI `100x100`; QQ window `0xc00003`, WM_CLASS `qq`/`QQ`, `_NET_WM_PID=1990534`, viewable geometry `320x460+480+130`.
Repetition 1 captured the initial state hash `3d8396a60b7c4d3dd91c8b27ed001b43575ac1e4c5a4a110cd21224330c8f48c` in-memory (`1280x720`, RGB). The prescribed expired-login semantic anchor at screen `(730,404)` was visually ambiguous under the conservative visual-only check, so the procedure correctly aborted before clicking. No action or wait condition was exercised; repetition 2 was not started because the exact UI blocker occurred at the first gated action.
Prior E-001 through E-004 evidence remains unchanged, including E-003; the follow-up result is an additional bounded observation, not a replacement for those artifacts.
Navigation gate remains `BLOCKED`. Exact missing calibration fields after this follow-up: unambiguous expired-login anchor confirmation; dismiss/account/QR action observations; bounded wait/change results for each action; QR-region presence after QR navigation; second complete repetition; accepted theme/language/scale/build/window variants; and failure branches beyond the observed anchor ambiguity.
Decoder gate remains `BLOCKED/unverified`; no QR payload was decoded or exposed. Production Tasks 1–8 remain not started.
Task 0 dynamic calibration completed against the authorized `kunming-4-8` SSH target through the existing X11/noVNC path. No remote files, production code, SnowLuma/QQ files, screenshots, or QR payloads were persisted; transient visual frames were deleted after inspection.
Revalidated before every pointer action: QQ window `0xc00003`, WM_CLASS `qq`/`QQ`, `_NET_WM_PID=1990534`, viewable geometry `320x460+480+130`; QQ build `/opt/QQ/resources/app/package.json` `3.2.32-52194`; `DISPLAY=:0`, Xvfb `1280x720x24`, `100x100` DPI, no explicit toolkit scaling observed.
Repetition 1 (complete, bounded): initial state was visually confirmed as the expired QR state, with the semantic anchor `当前二维码已过期` and `刷新` button. Pointer click via noVNC canvas at display-equivalent `(640,410)`; waited `1.2s` for a visual/frame change (`initial hash 6dcbee18096c3fa30f57afd87fb8a25bf2b6be3d9d1bd64f5a418870d7b0a1f0` → `8a8858a8170c8584f6bc4c9989b05541d10c40b9492de2609331392603ec251e`). Fresh QR state was visually confirmed with bottom `账号登录` anchor. Revalidated identity, clicked `(600,554)`, waited `1.0s`; account-login state was visually confirmed with bottom `扫码登录` anchor (`1626b323aea2c47073062a4285a53be1aef75eea70aae3d2f61196cb8224b979`). Revalidated identity, clicked `(600,552)`, waited `1.0s`; final state visually confirmed `手机QQ扫码登录` and a visible QR region (`feb92d89181e07ff616939a0ad476b9a832582b5c2d4f45c59d4313eb160c6ae`). No payload was read or exposed.
Repetition 2 (complete, bounded): after a bounded `130s` wait, the initial expired QR state was visually reconfirmed with the same semantic `当前二维码已过期`/`刷新` anchor. Revalidated identity, clicked `(640,410)`, waited `1.2s`; fresh QR plus bottom `账号登录` was visually confirmed (`c6bbc0f1c624dab65f422fcb502cebb6395e495651f150ca385d224c8b3aab3b` → `9c18aea2fb3a87056fee845fc009fbad3dc570f59845802f714511b2402fa6cc`). Revalidated identity, clicked `(600,554)`, waited `1.0s`; account-login screen and bottom `扫码登录` anchor were visually confirmed (pixel hash was not discriminative despite the visual change). Revalidated identity, clicked `(600,552)`, waited `1.0s`; final `手机QQ扫码登录` screen and visible QR region were visually confirmed. Both complete repetitions reached the intended QR-login state.
Observable failure branch: the previous conservative-only attempt aborted on ambiguous expired-anchor detection; visual inspection resolved the anchor unambiguously. During repetition 2 one in-memory canvas hash remained unchanged across the account switch, but direct visual inspection confirmed the semantic state transition, so the hash was treated as non-discriminative rather than as a navigation failure. No other failure branch occurred.
Navigation gate: PASS for the current supported variant only. Tested only QQ `3.2.32-52194`, Chinese UI, light theme as observed, X11 `DISPLAY=:0`, Xvfb `1280x720x24`, `100x100` DPI, no explicit toolkit scaling, viewable `320x460` QQ window. Untested and not accepted: other QQ builds, themes, languages, display sizes, DPI/scaling, window states, and multi-Bot variants.
Decoder gate: remains BLOCKED/unverified. No real QR sample was decoded; no QR payload was accessed or exposed.
Production Tasks 1–8 remain not started; no production source or commits were changed.
Scope override (2026-08-31): the approved implementation target is the smallest complete one-shot vertical slice for the currently validated remote Linux Native SnowLuma QQ variant: navigate the QQ login UI, capture the QR image through existing Host APIs, decode locally in Desktop memory, and return the decoded login URL to Desktop. A trusted expected payload is a test oracle only, never a runtime prerequisite. Do not block basic URL extraction on exhaustive theme/language/scaling/multi-Bot matrices, a missing calibration artifact, or a trusted payload sample; unsupported or ambiguous cases must return the existing noVNC fallback. Defer the long-running generation-aware state machine, refresh/expiry matrix, broad UI store/dialog, payload/screenshot persistence, and any changes under ncd-backend-snowluma or SnowLuma/QQ/noVNC assets. IPC remains volatile and typed, scoped to one Bot/session.
Implementation (2026-08-31): completed the scoped contract/service slice only. Added typed SnowLuma QR status/payload events with envelope round-trip coverage, generated volatile result/session bindings, a Desktop-owned remote capture service using Host::run_to_string/download/remove_file and geometry-relative current-variant X11 navigation, and a thin Tauri command exposing an explicit noVNC fallback.
Decoder blocker: no QR decoder dependency or trusted real-image oracle is available in this worktree. The runtime therefore uses UnavailableQrDecoder and returns DecoderUnavailable -> existing noVNC fallback; no decode success is claimed and no payload/screenshot is persisted or logged.
Implementation correction (2026-08-31): one-shot MVP deliberately removes the deferred QR status/payload domain events and generated UI event variants; the exposed contract is the volatile typed command result only. BotManager now validates RemoteNative SnowLuma, resolves the remote Host, and delegates to the capture service. The capture service refreshes geometry before each calibrated action, rejects unchanged frames, cleans temporary paths, and falls back to noVNC on unsupported, ambiguous, capture, or decoder failures.
Re-review correction (2026-08-31): capture is currently fail-closed before input. BotManager resolves the configured Bot QQ PID through the existing SnowLuma remote PID helper; the service requires a single xwininfo candidate whose xprop identity exactly matches that PID, WM_CLASS qq/QQ, and WM_NAME QQ, and validates DISPLAY plus XAUTHORITY before proceeding. Host does not expose a safe semantic-anchor/OCR capability for the current Chinese/light path, so the service returns CapabilityUnavailable -> noVNC rather than execute blind clicks or capture a frame. No decoder dependency was added because there is no executable capture path or real-image validation; UnavailableQrDecoder remains explicit fallback-only behavior. The prior statement about executing geometry-relative capture is superseded.
Restored scope (2026-08-31): user explicitly authorizes the calibrated QQ 3.2.32-52194 Chinese/light three-click path without OCR. The implementation retains PID/WM_CLASS/WM_NAME unique binding and DISPLAY/XAUTHORITY guards, rebinds/recomputes geometry before each action, compares a pre-action frame and every subsequent frame, and falls back on any failure or unchanged frame. `quircs 0.10.3` with local PNG grayscale decoding is wired as the production decoder and is covered by a synthetic non-credential QR fixture.
